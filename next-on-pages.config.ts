import { getRequestContext } from "@cloudflare/next-on-pages";

/** @type {import('next-on-pages').Config} */
const nextOnPagesConfig = {
  runtime: "nodejs_compat",
};

export default nextOnPagesConfig;
